import { IHalResource, IHalResourceConstructor } from "./hal-resource-interface";
import { HalRestClient } from "./hal-rest-client";
export declare class JSONParser {
    private halRestClient;
    constructor(halRestClient: HalRestClient);
    /**
     * convert a json to an halResource
     */
    jsonToResource<T extends IHalResource>(json: any, c: IHalResourceConstructor<T>, resource?: T, fetchedURI?: string): T;
    /**
     * parse a json to object
     */
    private parseJson;
    private extractURI;
}
