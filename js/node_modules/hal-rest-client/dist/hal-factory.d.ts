import { AxiosRequestConfig } from "axios";
import { IHalResource, IHalResourceConstructor } from "./hal-resource-interface";
import { HalRestClient } from "./hal-rest-client";
import { URI } from "./uri";
/**
 * create hal rest client
 * if a client with same base already exists, same client is returned
 *
 */
export declare function createClient(basename?: string, options?: AxiosRequestConfig): HalRestClient;
/**
 * create HalResource for uri
 */
export declare function createResource<T extends IHalResource>(client: HalRestClient, c: IHalResourceConstructor<T>, uri?: string | URI): T;
/**
 * reset cache for client or resource
 */
export declare function resetCache(): void;
