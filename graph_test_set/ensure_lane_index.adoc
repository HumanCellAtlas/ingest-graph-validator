## Test: ensure_lane_index

#### Test description

If more than one R1/R2/I1/I2 in a set, then ensure lane indices are specified.

If more than one set of R1/R2/I1/I2 SEQUENCE_FILEs belong to a single sequencing experiment PROCESS, than this snippet returns an error.


#### The test
[source,cypher]
----
/////////////////////////////////////////
// If more than one R1/R2/I1/I2 in a set,
// ensure lane index is specified.
MATCH (p:process)-[:DERIVED_FILES]->(f:sequence_file)
WHERE f.read_index IN [ 'read1', 'read2', 'index1', 'index2']
WITH
      p.`process_core.process_id` AS process_id
    , f.read_index AS read_index
    , collect(f.`file_core.file_name`) AS file_names
    , collect(f.lane_index) AS lane_indeces
    , count(*) AS fileCount
WHERE fileCount > 1 // more than one sequence file of any of type [ 'read1', 'read2', 'index1', 'index2'] belonging to a single experiment
  AND size(lane_indeces) <> fileCount //The totla number of sequence files and the number of sequence files with lane_index in each category must be equal to pass the test.
RETURN process_id, read_index, file_names, fileCount - size(lane_indeces) AS `FILES WITH ERRORS`, 'Whoops! Required lane_index is missing for some of the sequence files!!' AS message
ORDER BY process_id ASC, read_index ASC
/////////////////////////////////////////
----


[NOTE]
.Informaton about Graph Validation Tests
========================================
* Each cypher snippet in `.adoc` files in the specified folder will run.
* The cypher snippet should be contained between these specific lines:
```
#### The test
[source,cyper]
----
<TEST HERE>
----
```
* The test passes if the cypher query returns no results.
========================================
